@model IEnumerable<SCGroup>
@{ViewData["Title"] = "Strength & Conditioning";}
<div class="page-header">
    <div><h1 class="page-title">Strength & Conditioning</h1></div>
    <div class="d-flex gap-2">
        <a asp-action="RecordResult" class="btn btn-success btn-sm"><i class="fas fa-plus me-1"></i>Record Result</a>
        <a asp-action="Results" class="btn btn-outline-primary btn-sm"><i class="fas fa-list me-1"></i>Results</a>
        <a asp-action="CreateExercise" class="btn btn-outline-info btn-sm"><i class="fas fa-plus me-1"></i>Add Exercise</a>
        <a asp-action="CreateGroup" class="btn btn-outline-secondary btn-sm"><i class="fas fa-plus me-1"></i>Add Group</a>
    </div>
</div>
@foreach (var g in Model)
{
    <div class="card mb-3">
        <div class="card-header bg-white d-flex justify-content-between align-items-center">
            <h6 class="mb-0 fw-bold"><i class="fas fa-dumbbell me-2 text-primary"></i>@g.Name</h6>
            <a asp-action="CreateExercise" asp-route-groupId="@g.Id" class="btn btn-sm btn-outline-success"><i class="fas fa-plus me-1"></i>Add Exercise</a>
        </div>
        <div class="card-body">
            @if (g.Exercises.Any())
            {
                <div class="row g-2">
                    @foreach (var e in g.Exercises)
                    {
                        <div class="col-md-3">
                            <div class="border rounded p-2">
                                <div class="fw-semibold small">@e.Name</div>
                                @if(!string.IsNullOrEmpty(e.Measures)){<small class="text-muted d-block">@e.Measures</small>}
                                @if(!string.IsNullOrEmpty(e.MeasurementUnit)){<span class="badge bg-light text-dark" style="font-size:.7rem;">@e.MeasurementUnit</span>}
                            </div>
                        </div>
                    }
                </div>
            }
            else { <p class="text-muted small mb-0">No exercises added yet.</p> }
        </div>
    </div>
}
